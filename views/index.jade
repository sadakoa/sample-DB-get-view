extends layout

block content
  h1= title
  ul
    each val in docs
      li: a(href="")=val.name
        span: =val.id
        
//- ドキュメント作るのめんどくさかったらfor文使う

//- for (var i = 0; i < 10; i++) {
//-   db.aroe.insert({
//-     "name" : "user-" + i,
//-     "team" : i+1,
//-   });
//- }
